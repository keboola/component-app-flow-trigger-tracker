{
  "type": "object",
  "title": "Application configuration",
  "properties": {
    "output_list_flows": {
      "propertyOrder": 1,
      "type": "boolean",
      "format": "checkbox",
      "title": "Write list of flows with trigger to a output"
    },
    "trigger_ids": {
      "propertyOrder": 2,
      "title": "Flow triggers to reset",
      "type": "array",
      "description": "The ID of the trigger you wish to reset.",
      "enum": [],
      "items": {
        "enum": [],
        "type": "string"
      },
      "format": "select",
      "options": {
        "async": {
          "cache": true,
          "label": "Re-load flows with trigger",
          "action": "list_flows",
          "autoload": []
        }
      },
      "uniqueItems": true
    },
    "validation_button": {
        "type": "button",
        "format": "sync-action",
        "propertyOrder": 10,
        "options": {
          "async": {
            "label": "Validate",
            "action": "flow_detail",
            "autoload": ["properties.trigger_ids"]
          }
        }
      }
  }
}